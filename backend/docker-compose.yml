version: '3.8'
services:
  app:
    image: node:20 
    volumes:
      - .:/app
      - /app/node_modules
    working_dir: /app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    command: bash -c "npm install && npm start"

volumes:
  postgres_data:
